/**
 * Window definition
 */
declare interface Window {
	/**
	 * Duplicator Challenge Plugin
	 */
	dupChallengeData: {
		restRootUrl: string;
		rootDir: string;
		nonce: string;
		currentPage: string;
	};
}